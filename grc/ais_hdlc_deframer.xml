<?xml version="1.0"?>
<block>
  <name>hdlc_deframer</name>
  <key>ais_hdlc_deframer</key>
  <category>ais</category>
  <import>import ais</import>
  <make>ais.hdlc_deframer($frame_tag_name)</make>
  <param>
    <name>Frame tag name</name>
    <key>frame_tag_name</key>
    <type>string</type>
  </param>

  <!-- Make one 'sink' node per input. Sub-nodes:
       * name (an identifier for the GUI)
       * type
       * vlen
       * optional (set to 1 for optional inputs) -->
  <sink>
    <name>in</name>
    <type>byte</type>
  </sink>

  <!-- Make one 'source' node per output. Sub-nodes:
       * name (an identifier for the GUI)
       * type
       * vlen
       * optional (set to 1 for optional inputs) -->
  <source>
    <name>out</name>
    <type>message</type>
  </source>
</block>
