# auto-generated by grc.converter

id: ais_pdu_to_nmea
label: pdu_to_nmea
category: ais

parameters:
-   id: designator
    label: Designator
    dtype: string

inputs:
-   domain: message
    id: print
    optional: true
-   domain: message
    id: to_nmea
    optional: true

outputs:
-   domain: message
    id: out
    optional: true

templates:
    imports: import ais
    make: ais.pdu_to_nmea(${designator})

file_format: 1
